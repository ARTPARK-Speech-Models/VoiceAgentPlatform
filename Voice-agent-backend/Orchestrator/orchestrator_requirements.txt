annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
cffi==2.0.0
click==8.4.1
fastapi==0.137.0
h11==0.16.0
httptools==0.8.0
idna==3.18
numpy==2.4.6
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
soundfile==0.14.0
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.49.0
watchfiles==1.2.0
websockets==16.0
google-genai
psycopg2-binary
SQLAlchemy
firebase-admin
sarvamai
openai
alembic==1.13.3
pytest==8.4.2
httpx==0.28.1
ruff==0.8.4
--extra-index-url https://download.pytorch.org/whl/cpu
kokoro>=0.9.4
silero-vad
transformers
# DhVaani's zipvoice backend imports torch.utils.tensorboard at module load
# time (in an unused code path for inference), so this is required even
# though nothing here actually writes tensorboard logs.
tensorboard
# DhVaani zipvoice backend deps: vocos (vocoder), pydub (audio glue in its
# preprocessing). torchcodec deliberately NOT listed -- dhvaani.py patches
# torchaudio.load/save to use soundfile instead, so torchcodec's native lib
# (which fails to load here, missing libnvrtc.so.13) is never invoked.
vocos
pydub
torch
torchaudio
onnxruntime
# SraVaani-1.0's TDT tokenizer (sravaani_service.py)
sentencepiece
mcp[cli]>=1.2.0,<2